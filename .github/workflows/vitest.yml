name: Vitest CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  TEST_PORTAL_URL: ${{ secrets.TEST_PORTAL_URL }}
  TEST_PORTAL_API_KEY: ${{ secrets.VITEST_SECRET }}
  CLI_BRANCH: fix/send-logic
  BUILD_NAME: ${{ github.workflow }}
  BUILD_NUMBER: ${{ github.run_number }}
  BUILD_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
  REPOSITORY_NAME: ${{ github.repository }}
  REPOSITORY_URL: ${{ github.server_url }}/${{ github.repository }}
  BRANCH_NAME: ${{ github.head_ref || github.ref_name }}
  ENVIRONMENT: 'staging'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1 Step: Checkout main repo
      - name: Checkout main repo
        uses: actions/checkout@v4
        with:
          repository: sv-kisel/vitest-junit
          ref: main

      # 2 Step: Setup Node.js      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'

      # 3 Step: Configure npm auth for GitHub Packages
      - name: Configure npm auth for GitHub Packages
        run: |
          echo "@vention-test-portal:registry=https://npm.pkg.github.com" >> .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.PERSONAL_GITHUB_TOKEN }}" >> .npmrc
   
      # 4 Step: Install test reporter CLI        
      - name: Install test reporter CLI
        run: |
          npm install @vention-test-portal/test-portal-integration-cli@1.1.0

      # 5 Step: Run Vitest tests
      - name: Run Vitest
        run: npm run test
        continue-on-error: true

      # 6 Step: Upload Vitest report artifact
      - name: Upload Vitest report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vitest-report
          path: reports/vitest-report.json

      # 7 Step: Upload Test Results into Test Portal        
      - name: Upload Test Results
        if: always()
        run: |
          npx @vention-test-portal/test-portal-integration-cli upload \
            --input $GITHUB_WORKSPACE/reports/vitest-report.json \
            --type vitest \
